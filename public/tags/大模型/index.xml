<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>大模型 | Chongjie Si</title>
    <link>http://localhost:1313/tags/%E5%A4%A7%E6%A8%A1%E5%9E%8B/</link>
      <atom:link href="http://localhost:1313/tags/%E5%A4%A7%E6%A8%A1%E5%9E%8B/index.xml" rel="self" type="application/rss+xml" />
    <description>大模型</description>
    <generator>Hugo Blox Builder (https://hugoblox.com)</generator><language>en-us</language><lastBuildDate>Fri, 08 Aug 2025 00:00:00 +0000</lastBuildDate>
    <image>
      <url>http://localhost:1313/media/icon_hue006528a12cd6926ef214d6e0d83c455_38865_512x512_fill_lanczos_center_3.png</url>
      <title>大模型</title>
      <link>http://localhost:1313/tags/%E5%A4%A7%E6%A8%A1%E5%9E%8B/</link>
    </image>
    
    <item>
      <title>大模型训练3：从矩阵乘法看大模型如何切分</title>
      <link>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%833/</link>
      <pubDate>Fri, 08 Aug 2025 00:00:00 +0000</pubDate>
      <guid>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%833/</guid>
      <description>&lt;!--  $$ 
 --&gt;
&lt;p&gt;我们来聊聊大模型是怎么“拆分”（spilt or shard）到多个加速器上的。&lt;/p&gt;
&lt;p&gt;其实本质就是：大多数 LLM 主要由矩阵乘法组成，所以你只要搞懂“多个设备如何一起做矩阵乘法”，你基本就明白模型是怎么并行训练的了。接下来我们会基于 TPU 上的通信开销，给出一个简单直观的“分布式矩阵乘法”理论，帮助你理解这种跨设备的计算到底是怎么进行的。&lt;/p&gt;
&lt;h2 id=&#34;分区术语和集体操作&#34;&gt;分区术语和集体操作&lt;/h2&gt;
&lt;p&gt;当我们在一万个 TPU 上训练一个 LLM 时，其实本质上做的计算和在一个 TPU 上训练是一样的。不同的是——模型太大了，没法全都塞进单个 TPU 的 HBM（高带宽内存），所以我们不得不把数据“切开”。这个切开数组的过程，就叫做 “分片”（sharding）或 “划分”（partitioning）。&lt;/p&gt;
&lt;p&gt;举个例子，假设我们有一个二维数组  $A$ 
，它被平均分成 4 份，分布在 4 个 TPU 上。从“全局”角度看，这个数组还是原来的大小，比如说是 (4, 128)；但从每个设备本地看，每块只有 (2, 64)，也就是说每个 TPU 实际只存了一小部分——总共 1/4 的数据。&lt;/p&gt;
&lt;img src=&#34;https://pic2.zhimg.com/v2-6aefdd0beacc7518906c3e42b473eac1_1440w.jpg&#34;&gt;
&lt;p&gt;接下来我们会把这个想法推广到任意维度的数组，并介绍在多设备之间操作这些分片数组时要用到的一些“集体操作”（collective operations），比如 all-reduce、all-gather 等等。&lt;/p&gt;
&lt;h3 id=&#34;统一的分片表示法&#34;&gt;统一的分片表示法&lt;/h3&gt;
&lt;p&gt;我们用一种 “命名轴” (named-axis notation) 的变体记法来描述张量是如何在多个设备之间以块（block）方式分片（shard）的。具体来说，我们假设设备之间构成了一个二维或三维的网格（device mesh），这个网格的每个维度都有一个名字，比如  $X$ 
、 $Y$ 
 或  $Z$ 
。接着，我们可以通过描述数组的哪些维度沿着哪些设备网格的轴进行了划分，来指定这个张量是如何被分布式地存储的。这个映射方式就叫做 Sharding（分片策略）。&lt;/p&gt;
&lt;p&gt;我们以之前的图为例，我们有：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Sharding 表达式：  $A[I_X, J_Y]$ 
。这句话的意思是：数组  $A$ 
 的第一个维度  $I$ 
 被分布在设备网格的  $X$ 
 轴上，第二个维度  $J$ 
 被分布在  $Y$ 
 轴上。换句话说， $I$ 
 维度按  $X$ 
 分片， $J$ 
 维度按  $Y$ 
 分片，那么最终的结果就是，每个设备上只保存整个数组的  $1/(|X|*|Y|)$ 
 部分。&lt;/li&gt;
&lt;li&gt;设备网格 Mesh ：设备网格定义如下，这表明我们有 4 个 TPU，排成一个 2×2 的二维网格，其中第一个维度叫  $X$ 
，第二个维度叫  $Y$ 
 。&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-python&#34; data-lang=&#34;python&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;n&#34;&gt;Mesh&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;(&lt;/span&gt;&lt;span class=&#34;n&#34;&gt;devices&lt;/span&gt;&lt;span class=&#34;o&#34;&gt;=&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;((&lt;/span&gt;&lt;span class=&#34;mi&#34;&gt;0&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;,&lt;/span&gt; &lt;span class=&#34;mi&#34;&gt;1&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;),&lt;/span&gt; &lt;span class=&#34;p&#34;&gt;(&lt;/span&gt;&lt;span class=&#34;mi&#34;&gt;2&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;,&lt;/span&gt; &lt;span class=&#34;mi&#34;&gt;3&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;)),&lt;/span&gt; &lt;span class=&#34;n&#34;&gt;axis_names&lt;/span&gt;&lt;span class=&#34;o&#34;&gt;=&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;(&lt;/span&gt;&lt;span class=&#34;s1&#34;&gt;&amp;#39;X&amp;#39;&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;,&lt;/span&gt; &lt;span class=&#34;s1&#34;&gt;&amp;#39;Y&amp;#39;&lt;/span&gt;&lt;span class=&#34;p&#34;&gt;))&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;ul&gt;
&lt;li&gt;本地分片大小（local shape）：假设数组  $A$ 
 的全局维度是  $(I,J)$ 
 ，那么每个设备拿到的本地数据大小就是  $(I/2, J/2)$ 
 。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;总结一下：通过指定  $A[I_X,J_Y]$ 
 和设备网格的结构，我们就知道每个 TPU 拿到数组  $A$ 
 的哪一部分、拿多少。这种方式很适合大模型训练中手动设计 sharding 策略，尤其在 TensorFlow XLA、JAX、TPU 等系统中非常常见。你可以想象成是“把数据按指定方向切开、分别塞进每个计算核心”。&lt;/p&gt;
&lt;p&gt;我们再给一个2D 张量在单个轴上进行 2D 分片的例子来更加直观的感受一下： $A[I_{XY},J]$ 
 。这里的意思是：张量  $A$ 
 的第一维  $I$ 
 被同时沿着设备网格的  $X$ 
 和  $Y$ 
  两个方向进行分片；第二维  $J$ 
  没有分片，所有设备都完整保留这个维度；这跟前面的  $A[I_X,J_Y]$ 
  相比，不是同时沿两个张量维度分片，而是把同一个维度  $I$ 
 ，在设备网格的两个方向上切了。&lt;/p&gt;
&lt;p&gt;这种分片方式在一些大模型里非常实用，尤其是当我们只想并行某个维度的计算，但又希望利用多个方向的设备拓扑结构（比如在 TPU 的  $X$ 
  和  $Y$ 
  轴上都利用上）。&lt;/p&gt;
&lt;p&gt;好的，again，加深印象，让我们再来可视化一下这些不同的sharding 是怎么把一个二维数组分配到 4 个设备上的。&lt;/p&gt;
&lt;img src=&#34;https://pic2.zhimg.com/v2-0798764aca58ec4aa35c64f7f3ef8263_1440w.jpg&#34;&gt;
&lt;p&gt;我们可以完全复制（Fully Replicated），写作：  $A[I,J]$ 
 （没有任何分片标记），这个张量在每个设备上都有一份完整的副本。&lt;/p&gt;
&lt;img src=&#34;https://pic4.zhimg.com/v2-c7f0947d0d24a2704c8dcde1d934598d_1440w.jpg&#34;&gt;
&lt;p&gt;当我们想表示张量的某个维度已经被 shard到某个 mesh 轴（硬件设备网格的某一维）上时，就会在该维度下面加一个 mesh 轴的下标。比如这个写法：  $A[I_X,J]$ 
，表明  $I$ 
 被划分到了 mesh 的  $X$ 
 轴上，而  $J$ 
 没有被划分。&lt;/p&gt;
&lt;img src=&#34;https://pica.zhimg.com/v2-32979a20e21b5f40caa15281951c7c78_1440w.jpg&#34;&gt;
&lt;p&gt;那么  $A[I_X,J_Y]$ 
 的示意图也就比较好理解了：&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-f67591e28038f188d951854e656e7629_1440w.jpg&#34;&gt;
&lt;p&gt;我们也列举了一些其他可能的情况：&lt;/p&gt;
&lt;img src=&#34;https://pica.zhimg.com/v2-28b75ec591e05611574ecaf2324ae040_1440w.jpg&#34;&gt;
&lt;p&gt;这里， $A[I_{XY},J]$ 
 表示我们把 mesh 轴  $X$ 
 和  $Y$ 
 组合成一个更大的一维（flattened）维度，然后将张量的轴  $I$ 
 按照这个展开后的维度来切分，也就是说： $I$ 
 轴的数据会被平均分布到  $X\times Y$ 
 的所有设备上；  $J$ 
 轴的数据没有被划分，会在每个设备上完整保留。&lt;/p&gt;
&lt;p&gt;最后要注意的是：同一个 mesh 轴不能同时用于切分多个张量维度。比如这个写法： $A[I_X,J_X]$ 
  就是不合法的，因为你试图把轴  $I$ 
 和  $J$ 
 都沿着 mesh 的  $X$ 
 维度切分。直白说就是：一条 mesh 轴（比如  $X$ 
 ）在某个张量维度上用过之后，就相当于“用完”了，不能再用来切别的维度了。这是因为每个 mesh 维度只能映射到一个逻辑维度的切分上，否则就会发生冲突 —— 同一个设备就不知道该接收哪个维度的数据了。这在实际的模型并行中会导致资源分配混乱或者通信无法对齐。&lt;/p&gt;
&lt;h2 id=&#34;分布式数组的计算&#34;&gt;分布式数组的计算&lt;/h2&gt;
&lt;p&gt;当你的数据被切分在多个设备上，你还想对它做数学运算时，一个核心问题是：在切分数据和并行计算的前提下，额外代价来自哪里？答案其实取决于你要做的操作类型：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;元素级运算（elementwise ops）：比如加法、ReLU、逐元素乘法……这些操作每个元素自己算自己的，不需要跨设备交流。所以基本没代价，在哪个设备上的元素就在哪算，完事儿。&lt;/li&gt;
&lt;li&gt;涉及跨元素/跨设备的运算（比如矩阵乘法）：这就复杂了。但好消息是：深度学习的大部分重计算，其实就是矩阵乘法。而矩阵乘法的通信代价是可以系统分析的，我们接下来就只关注它。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;我们现在正式进入重点：如何对「被切分」的矩阵进行乘法？&lt;/p&gt;
&lt;p&gt;你可以把整个过程理解成：把矩阵的碎片搬来搬去，然后在每台设备上单独做乘法，最后把结果拼起来。这背后的关键问题是：怎么切分矩阵，决定了你需不需要通信（也就是设备之间要不要发数据）。比如：&lt;/p&gt;
 $$A[I_x, J]  \cdot B[J, K_y] \rightarrow C[I_x, K_y]$$ 

&lt;p&gt;这时候： $A$ 
 的第一个维度  $I$ 
 被切在了  $X$ 
  轴， $B$ 
 的第三个维度  $K$ 
  被切在了  $Y$ 
  轴； $J$ 
  没被切，是“完整的”；最后的输出  $C$ 
  也被切分了。不过好消息是，  $J$ 
  是乘法中需要 sum 的那一维（contracting dim），没被切开，就不用通信。也就是说，每台设备本地拿到的数据就能完成计算，不用跟别人说话，效率高。&lt;/p&gt;
&lt;p&gt;但如果你想让输出   $C$ 
  是完整的、不切的，比如：&lt;/p&gt;
 $$A[I_x, J] \cdot B[J, K_y] \rightarrow C[I, K]$$ 

&lt;p&gt;那问题就来了，你得让每台设备把结果发出去，或者让别人把  $A$ 
  发进来，反正得有通信。而且你有两种选择：拷贝  $A$ 
 ，或者拷贝  $C$ 
 ，哪种划算得算一下。现在我们把所有可能的切法总结成 4 种情况，每种都有对应的通信策略：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;没有任何输入在 sum 的维度上被切；&lt;/li&gt;
&lt;li&gt;有一个输入在 sum 的维度上被切；&lt;/li&gt;
&lt;li&gt;两个输入都在 sum 维度上被切；&lt;/li&gt;
&lt;li&gt;两个输入都在“非 sum 的维度”上按同一个轴被切。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;好，已经绕晕了，可能看半天也不知道啥意思。没关系，我们接下来一个一个来看。&lt;/p&gt;
&lt;p&gt;在开始之前，建议带着矩阵分块乘法的视角。&lt;/p&gt;
&lt;h3 id=&#34;case-1两个矩阵的输入都没有在被求和的维度上做切分&#34;&gt;Case 1：两个矩阵的输入都没有在「被求和的维度」上做切分&lt;/h3&gt;
&lt;p&gt;这其实是最轻松的一种情况。我们先看个定理（放心，不难）：&lt;/p&gt;
&lt;p&gt;当你在做矩阵乘法时，只要「1. 被求和的维度没被切开」，并且「2. 两个矩阵的输入在非求和维度上不切在同一轴」，那计算就能顺利进行，而且输出的切分规则也会自动匹配输入的切法。&lt;/p&gt;
&lt;p&gt;有点晦涩绕口，举个例子： $A[I_x, J] \cdot B[J, K_y] \rightarrow C[I_x, K_y]$ 
&lt;/p&gt;
&lt;p&gt;在这个例子里：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt; $J$ 
 是求和的维度； $J$ 
没有被切开；&lt;/li&gt;
&lt;li&gt; $I$ 
 和  $K$ 
 不是求和维度，并且没有在同一个mesh 轴上切分。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;所以每台设备都可以拿到自己那一块的  $I$ 
 和  $K$ 
 的数据， 然后在本地用完整的  $J$ 
 做乘法和 sum。这完全不需要通信，因为直接在各自的设备上就可以把结果算出来，输出自动是按照  $X$ 
 和  $Y$ 
 轴切分的。这也是为什么这种情况被称为最理想的情况：没有通信，只有计算，本地就能搞定一切。&lt;/p&gt;
&lt;p&gt;一句话总结：如果求和的维度没被切，乘法就能本地算完，不用通信，省时省力还高效。&lt;/p&gt;
&lt;h3 id=&#34;case-2只有一个矩阵的被求和的维度被切分了&#34;&gt;Case 2：只有一个矩阵的「被求和的维度」被切分了&lt;/h3&gt;
&lt;p&gt;假设我们要做一个矩阵乘法：  $A[I, J_x] \cdot B[J, K] \rightarrow C[I, K]$ 
。也就是说，矩阵  $A$ 
 在  $J$ 
 这个被求和的维度上被按  $X$ 
 轴切分了，而  $B$ 
 是完整复制的。这种情况下，我们不能直接让每个设备拿着自己本地的  $A$ 
 和  $B$ 
 就地做乘法，因为  $A$ 
 的每台设备只持有  $J$ 
 的一部分，而我们要求的是把  $J$ 
 全部都 sum 一遍。如果你少了  $J$ 
 的一部分，那这个 sum 出来的结果就不对。&lt;/p&gt;
&lt;p&gt;为了解决这个问题，我们要做的是“先 AllGather 再乘”： ${\rm AllGather}_x(A[I, J_x]) \rightarrow A[I, J]$ 
 ， $ A[I, J] \cdot B[J, K] \rightarrow C[I, K] $ 
 。也就是说：我们把  $A$ 
 沿着切分的维度上的碎片 gather 回来，每个设备都拿到  $A$ 
 的完整副本，然后再和  $B$ 
 做乘法。AllGather 的作用就是“把沿某个轴切分的数据，重新收集成完整的副本”。当然，我们也可以只 gather 掉一部分，比如： $A[I_{xy}, J] \rightarrow A[I_{y}, J]$ 
。&lt;/p&gt;
&lt;p&gt;注意，AllGather 也可以用于非求和维度，比如我们本来有： $A[I_x, J] \cdot B[J, K] \rightarrow C[I, K]$ 
。虽然  $J$ 
 没被切（很好），但  $I$ 
  被切了，我们如果想要最后的输出  $C$ 
  是不切分的，也可以： ${\rm AllGather}_x(C[I_x, K]) → C[I, K]$ 
 。但这时候我们有选择权，可以在乘法前或乘法后再 gather，没强制。&lt;/p&gt;
&lt;p&gt;这里还有几个小问题：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;AllGather 是怎么被执行的？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;我们前面说了，AllGather 的目的是把每台设备手里那一小块数据互相传一传，最后每台设备都拥有完整的数据。那这个“互相传一传”到底怎么做的？&lt;/p&gt;
&lt;p&gt;想象你有一圈设备，每台都有一小份数据。比如有 8 台设备，每台有数组的 1/8，我们的目标是让每一台设备最终拥有 shard 0~7 的全量副本。那么我们有两种方式：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;单向传输：每台设备只往一个方向（比如顺时针）传：D0 → D1 → D2 → … → D7 → D0（回到起点），每轮，每台设备把新收到的 shard 再往下传，总共需要 N - 1 轮（因为第一个 shard 你自己已经有了）；每一跳传的数据大小是：  ${\rm total \ size}/N$ 
 。&lt;/li&gt;
&lt;li&gt;双向传输：每台设备同时往前后两个方向传，也就是D0 同时传给 D1 和 D7，D1 同时传给 D2 和 D0，以此类推。这样做传输次数可以减半：最多只需要  ${\rm ceil}(N/2)$ 
  跳。但每一跳要传两个 shard 的大小： $2*{\rm total \ size} / N$ 
 。&lt;/li&gt;
&lt;/ul&gt;
&lt;img src=&#34;https://pic1.zhimg.com/v2-1cf9e884d543715f3a29a36ded15d7f8_b.webp&#34;&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;AllGather 要花多久时间？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;我们现在来算一下用“双向传输”的 AllGather 大概要花多久。假设：你的总数组大小是  $V$ 
  字节，沿着某个维度被分成了  $|X|$ 
  份（即有  $|X|$ 
  台设备分着这个数据），每一台设备之间的通信带宽是  $W_{ICI}$ 
  。每一跳会向两个方向传  $V/|X|$ 
  个字节，所以每一跳花的时间是   $T = \frac{2\cdot V}{|X|\cdot W_{ICI}}$ 
 。由于我们是双向传输，所以只需要跳  $|X| / 2 $ 
  次就能传遍所有设备，因此，总时间为：&lt;/p&gt;
  $$T_{total} = \frac{2\cdot V \cdot |X|}{2\cdot |X| \cdot W_{ICI}} = \frac{V}{W_{ICI}}$$ 

&lt;p&gt;是的，你没看错。这个公式里根本没有设备数量  $|X|$ 
 。这意味着，不管你 AllGather 是 4 台、8 台、32 台——只要链路带宽一样、数据量一样，总花费时间是一样的。虽然设备越多跳得多，但每次跳的数据就变少了，两者正好抵消。&lt;/p&gt;
&lt;p&gt;总结一句话： 在带宽主导的场景下，AllGather（或 AllReduce、ReduceScatter）真正决定速度的，是你传了多少数据和你那条线的带宽，不是设备数量。&lt;/p&gt;
&lt;ol start=&#34;3&#34;&gt;
&lt;li&gt;关于 ICI 延迟的一些注意点&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;虽然之前我们说 AllGather 的时间跟设备数量无关，但有个例外：当数据量特别小的时候。这是因为每次跳都有一个固定的延迟开销，大概是 1 微秒，不管你传多少字节。如果每次跳都只传一点点数据，那每次传输用的时间其实不是带宽限制，而是被这个 1 微秒的固定开销给主导了。这种情况我们就叫：Latency-bound（延迟主导） 而不是 Throughput-bound（带宽主导）。也就是说，数据太小的时候，哪怕你带宽很快，还是慢！因为你光是“发起一次跳”的时间都比传输时间还长了。&lt;/p&gt;
&lt;p&gt;我们可以在 TPU v5e 的一个 8×16 的 slice 上做一个实验：把数组沿着 16 的维度切开，然后做 AllGather，正好是个完整的双向环。下面是结果图：&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-c3b604b09930e3a2d0645ef8eea65495_1440w.jpg&#34;&gt;
&lt;ul&gt;
&lt;li&gt;橙色线：实际 AllGather 成功的带宽（多少数据每秒被收集起来）&lt;/li&gt;
&lt;li&gt;蓝色线：推测的单向链路带宽（根据硬件模型反推出来的）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;结论很直白：实际能用到的带宽，大概只有理论峰值（4.5e10）的 95%&lt;/p&gt;
&lt;p&gt;这个带宽只有当数组大小到达 10MB 左右时才能稳定发挥，10MB/16 = 每台设备大概 500KB 数据的时候，性能才最稳。&lt;/p&gt;
&lt;p&gt;一句话总结：当数据特别小（比如每台设备不到 500KB）的时候，AllGather 会受到 延迟 而不是 带宽 的限制，导致变慢。所以，如果你做的是超小 batch 的操作，记得别指望通信能快。&lt;/p&gt;
&lt;h3 id=&#34;case-3两个矩阵都在被求和的维度上做了切分&#34;&gt;Case 3：两个矩阵都在「被求和的维度」上做了切分&lt;/h3&gt;
&lt;p&gt;设我们要计算： $A[I, J_x] \cdot B[J_x, K] \rightarrow C[I, K]$ 
，这意味着：   $A$ 
 和   $B$ 
  都在   $J$ 
  维做了分片。计算本地矩阵乘法当然可以做（因为每台设备都有   $J_x$ 
  的局部部分），但每个设备只能算出“部分和”（partial sum），它看不到完整的  $J$ 
  ，所以它算的乘法结果也是不完整的。我们把这种不完整的结果写作：&lt;/p&gt;
 $$A[I, J_x] \cdot {\rm _{Local}}B[J_x, K] \rightarrow C[I, K]\{U_x\}$$ 

&lt;p&gt;这里的   $\{U_x\}$ 
 表示结果还“没求和完”（unreduced），要进一步合并。那怎么让这些 partial sum 合起来？这就牵扯到一个操作，叫 AllReduce。AllReduce 会让所有设备把自己算出来的部分结果发给邻居，然后大家互相累加，最后所有设备都得到了完整的和（full sum）：  ${\rm AllReduce}_X C[I, K]\{U_x\}  \rightarrow C[I, K]$ 
 。&lt;/p&gt;
&lt;p&gt;同样的，我们也有关于 AllReduce 的几个问题。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;AllReduce 到底有多“贵”？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;想象一下你现在有好多台 TPU，每台上都有一个部分结果（shard），我们现在想要每台设备都拿到这个结果的总和。那么每台设备要把自己的结果发给别的设备，同时接收别人的，然后自己把所有这些结果加在一起，每台设备最后都有一个完整的总和。这个过程比 AllGather 要贵，因为在 AllGather 中，每台设备只需要收集别人的一小部分数据。但在 AllReduce 里，每个 shard 本身就是一个“全量大小”的数组（跟最终结果一样大），所以传输的数据量是 AllGather 的两倍左右。&lt;/p&gt;
&lt;p&gt;我们也可以把 AllReduce 拆成两个步骤理解：1. ReduceScatter：先把每台设备上的“部分和”加起来，然后把结果按设备数切片，分发出去（每台设备只保留其中一块）；2. AllGather：再把这些分散的部分收集回来，让每台设备都拿到完整结果。这两个操作合起来，就等于 AllReduce。所以可以理解为：AllReduce ≈ 2 × AllGather 的开销。&lt;/p&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;关于ReduceScatter&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;回顾一下 AllReduce 做了两件事：把所有设备的部分结果加起来，再对总的结果切分，分发到每台设备；聚合所有设备的结果。而 ReduceScatter 只做了第一步 —— 加完之后，每台设备只保留自己那一部分，而不是拿到全部数据。换句话说，原本你有一堆“没加完的结果”，ReduceScatter 负责把它们加完，然后再把最终结果按设备分开发下去（scatter）。所以最后的数组不再是部分和状态，而是已经按设备分好了的结果。这么说还是有点抽象，我给一个例子。假设我们有一个乘法&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-markdown&#34; data-lang=&#34;markdown&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;# A: [I, J], B: [J, K]
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;&lt;/span&gt;C = A · B
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;ReduceScatter 的做法是：&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-markdown&#34; data-lang=&#34;markdown&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;ReduceScatter (比如我们选择 K 维 sharding)：
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 0 最后保留 C[:, 0:K/4]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 1 最后保留 C[:, K/4:K/2]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 2 最后保留 C[:, K/2:3K/4]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 3 最后保留 C[:, 3K/4:K]
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;而并非每台设备上原有的结果：&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-markdown&#34; data-lang=&#34;markdown&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 0: C0 = A0 · B0，[I, K]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 1: C1 = A1 · B1
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 2: C2 = A2 · B2
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 3: C3 = A3 · B3
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;你可以理解成：我们一边把结果加起来（Reduce），一边按  $K$ 
 切块丢到不同设备（Scatter）。 另外，ReduceScatter 跟 AllGather 很像，其实 ReduceScatter 的通信方式也跟 AllGather 很像，区别在于：AllGather 是每台设备保留每一块；ReduceScatter 是每台设备把东西加一加，然后只留自己那一块。所以两者的延迟（latency）也差不多，除了加法这一步要多花点时间。&lt;/p&gt;
&lt;img src=&#34;https://pic3.zhimg.com/v2-734b70432d6c0c3d89e4bfabe07bd152_b.webp&#34;&gt;
&lt;h3 id=&#34;case-4两个矩阵的非被求和的维度被沿着同一个-mesh-轴切分了&#34;&gt;Case 4：两个矩阵的「非被求和的维度」被沿着同一个 mesh 轴切分了&lt;/h3&gt;
&lt;p&gt;在实际情况中，会有一种“你不能这么干”的情况，比如这样：&lt;/p&gt;
 $$A[I_X, J] \cdot B[J, K_X] \rightarrow C[I_X, K_X]$$ 

&lt;p&gt;这为什么不行？&lt;/p&gt;
&lt;p&gt;想象下，有一组设备都沿着  $X$ 
 轴做了切分。那每个设备  $i$ 
 就只拿到结果矩阵  $C$ 
 中第  $(i,i)$ 
 个对角线位置的 shard。换句话说，每个设备只知道自己那一点的信息，完全不知道别的设备上算出来的部分。所以整个矩阵只知道对角线，其他值全是未知的。 这就没法还原完整的结果，所以这个切法是非法的。&lt;/p&gt;
&lt;p&gt;为了解决这个问题，我们可以先 AllGather 一些维度。具体来说，我们有两种选择：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;AllGather A：  ${\rm AllGather}_X A[I_X, J] \rightarrow A[I, J],\ \ A[I, J] · B[J, K_X] \rightarrow C[I, K_X]$ 
&lt;/li&gt;
&lt;li&gt;AllGather B：   ${\rm AllGather}_X B[J, K_X] \rightarrow B[J, K], \ \ A[I_X, J] · B[J, K] \rightarrow C[I_X, K]$ 
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;也就是说，我们必须让  $X$ 
 只出现一次，不能同时出现在  $A$ 
 和  $B$ 
 的非收缩维上。哪个维度 AllGather，要看你后面想怎么继续切。&lt;/p&gt;
&lt;h2 id=&#34;更深入地理解-tpu-的通信原语&#34;&gt;更深入地理解 TPU 的通信原语&lt;/h2&gt;
&lt;p&gt;我们在前面四种矩阵乘法的分片（sharding）场景中，其实已经接触了几种非常关键的通信操作：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;AllGather：合并分片，用来取消某个维度的切分，把各个设备手上的分片合成完整数据。&lt;/li&gt;
&lt;li&gt;ReduceScatter：求和并重新分片，用来“去掉未归约的后缀”（比如   $\{U_X\}$ 
 ），但并不会把数据变成完全复制的，而是重新分片到另一个维度上。&lt;/li&gt;
&lt;li&gt;AllReduce：全量求和 + 广播，它最后会让每个设备上都拥有完整的求和结果。换句话说，ReduceScatter 是 “算完再分”，AllReduce 是 “算完每人都留一份”。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;然而，还有最后一个通信原语——AllToAll，这个在我们之前讨论的几种矩阵乘法中还没出现，但它在 MoE（专家混合模型） 和某些其他需要复杂路由的计算中非常常见。&lt;/p&gt;
&lt;h3 id=&#34;alltoall&#34;&gt;AllToAll&lt;/h3&gt;
&lt;p&gt;简单理解，AllToAll 就是一种跨设备的“转置”或“重新分片”操作。它的本质是在多个设备之间，把数据从一个维度的切分方式，重新映射到另一个维度。比如原来是按 batch 切的，现在要按 expert 分，那就需要 AllToAll 来调整这些 shard 的布局。&lt;/p&gt;
&lt;p&gt;举个例子，假设你有 8 个 TPU 芯片，原来是每个芯片拿一块 batch 的数据（比如按   $I$ 
 维切片），现在你要让每个芯片专门负责一部分 expert（比如按   $J$ 
 维切片），那就得把   $I$ 
 维的分片“转”到   $J$ 
 维，这时候就得用 AllToAll。你可以把 AllToAll 想象成把张量的一个下标从一个维度“搬”到另一个维度。&lt;/p&gt;
&lt;p&gt;AllToAll 和 AllGather 最大的不同是：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;AllGather 是把每个分片的数据 复制到所有设备，每个设备最后都会拿到全量；&lt;/li&gt;
&lt;li&gt;AllToAll 是互相交换分片的一部分，但每个设备只拿自己需要的那块数据，不做全量复制。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;正因为 AllToAll 不用复制所有 shard 的内容，所以它的通信成本其实比 AllGather 要低不少——通常 AllToAll 的通信开销是 AllGather 的 四分之一。这在需要频繁 reshuffle 数据布局的模型中（比如 MoE）能省下不少代价。&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-0de83734c733ad136306ed16315c3989_b.webp&#34;&gt;
&lt;h3 id=&#34;更深入聊聊-reducescatter&#34;&gt;更深入聊聊 ReduceScatter&lt;/h3&gt;
&lt;p&gt;虽然听起来它只是众多通信操作之一，但其实 ReduceScatter 是 AllGather 的“反操作”，两者是一对好搭档。举个简单例子：正向传播时我们用 AllGather 把各个设备的分片拼成完整张量；反向传播时我们需要把梯度重新按分片打散，那就用 ReduceScatter 把这些结果 “散回去”。你可以把它想象成：“AllGather 是拼图，ReduceScatter 是拆图。”&lt;/p&gt;
&lt;p&gt;我们用公式来表示一下，  ${\rm AllGather}_X A[I_X] \rightarrow A[I]$ 
 ，意思是原来  $A$ 
 被按  $I$ 
 维在  $X$ 
 轴上分片了，现在我们 AllGather 一下，把它拼成完整的  $A[I]$ 
 。反过来， ${\rm ReduceScatter}_X A’[I]\{U_X\} \rightarrow A’[I_X]$ 
，就是我们把反向的梯度   $A&#39;[I]$ 
 拆成分片状态。&lt;/p&gt;
&lt;p&gt;那么为什么我们需要ReduceScatter？这是因为有时候我们其实不想立刻 AllGather 把整个张量都还原，因为那太耗资源了。比如下面这种情况：&lt;/p&gt;
 $$A[I, J_X] \cdot B[J_X, K] \rightarrow C[I, K_X]$$ 

&lt;p&gt;两个输入在  $J$ 
 维度上都分片了，我们本来是要 AllReduce 来合并结果。但有时候可以：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;先 LOCAL 做乘法；&lt;/li&gt;
&lt;li&gt;再 ReduceScatter 把结果拆成分片状态；&lt;/li&gt;
&lt;li&gt;如果以后需要再 AllGather，再做也不迟！&lt;/li&gt;
&lt;/ol&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-markdown&#34; data-lang=&#34;markdown&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Step 1: LOCAL 乘法（每个设备做自己那份）：
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 0: C0 = A0 · B0
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 1: C1 = A1 · B1
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 2: C2 = A2 · B2
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Device 3: C3 = A3 · B3
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;Step 2: ReduceScatter (比如我们选择 K 维 sharding)：
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 0 最后保留 C[:, 0:K/4]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; Device 1 最后保留 C[:, K/4:K/2]
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;k&#34;&gt;-&lt;/span&gt; ...
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;这样你就可以一直保持 tensor 是分片的状态，不用每一步都还原成全量，更省显存，更高效。&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-markdown&#34; data-lang=&#34;markdown&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;# 假设 A 和 B 是按 J 分片的矩阵
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;# 原始方式（AllReduce）：
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;&lt;/span&gt;C_partial = local_matmul(A_local, B_local)
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;C = all_reduce(C_partial)  # 得到全量的 C
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;# 更高效的方式（ReduceScatter）：
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;&lt;span class=&#34;gh&#34;&gt;&lt;/span&gt;C_partial = local_matmul(A_local, B_local)
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;C_sharded = reduce_scatter(C_partial, axis=&amp;#39;K&amp;#39;)  # 直接输出分片结果
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;总结一句话：ReduceScatter 不仅是 AllGather 的逆操作，它还能让你控制结果接下来如何被分片，是大规模并行计算里的关键角色。&lt;/p&gt;
&lt;h2 id=&#34;经典总结&#34;&gt;经典总结&lt;/h2&gt;
&lt;p&gt;好了，晦涩的文字到此结束，总结一下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;什么是 Sharding？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;一个数组的分片方式由两个部分决定：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Mesh：定义了 TPU 硬件的物理轴（比如  $X$ 
、 $Y$ 
），比如 Mesh({&amp;lsquo;X&amp;rsquo;: 4, &amp;lsquo;Y&amp;rsquo;: 8}) 表示 4x8 的设备网格。&lt;/li&gt;
&lt;li&gt;Sharding：指定了数组的哪些逻辑维度要分布到哪些 Mesh 轴上。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;例如， $A[I_{XY},J]$ 
 表示  $A$ 
 的第一个维度被按  $X$ 
 和  $Y$ 
 轴一起分成 32 份。&lt;/p&gt;
&lt;ol start=&#34;2&#34;&gt;
&lt;li&gt;带分片的数组怎么做计算？&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;跟没分片时类似，但如果要对被分片的维度做矩阵乘法，就需要通信操作。我们可以把情况分成以下 4 类：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;没有分片在被求和维度上： 不需要任何通信操作。&lt;/li&gt;
&lt;li&gt;只有一个矩阵在求和维度上分片： 用 AllGather 把某个输入先聚合起来再做计算。&lt;/li&gt;
&lt;li&gt;两个矩阵都在求和维度上分片：每个设备先做本地计算，再用 AllReduce 或 ReduceScatter 聚合结果。&lt;/li&gt;
&lt;li&gt;两个矩阵在非求和维度上用了相同的 mesh 轴做分片：不合法，必须 AllGather 其中一个数组。&lt;/li&gt;
&lt;/ul&gt;
&lt;ol start=&#34;3&#34;&gt;
&lt;li&gt;TPU 上的 4 个核心通信操作&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;TPU 中有 4 个基础通信原语（primitive）来支持分布式矩阵乘法：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;AllGather：取消某个维度上的分片，把数据全 gather 到每个设备;&lt;/li&gt;
&lt;li&gt;ReduceScatter：对某个维度上的分片做加和，然后再进行分片;&lt;/li&gt;
&lt;li&gt;AllReduce：先 ReduceScatter 再 AllGather（其实是组合操作）;&lt;/li&gt;
&lt;li&gt;AllToAll：改变分片的布局，即把分片从一个维度换到另一个。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这一章写起来是真不容易，写完后才发现已经是万字长文了。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>大模型训练2：TPU是如何工作的？</title>
      <link>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%832/</link>
      <pubDate>Mon, 04 Aug 2025 00:00:00 +0000</pubDate>
      <guid>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%832/</guid>
      <description>&lt;!--  $$ 
 --&gt;
&lt;p&gt;今天主要来讲一讲 TPU 是怎么工作的，它们之间是如何联网的，从而支持多芯片的训练和推理。这些结构和机制会直接影响你常用算法的性能表现。当然，即使你是用 GPU 的，也有很多有用的内容值得参考。&lt;/p&gt;
&lt;h2 id=&#34;什么是-tpu&#34;&gt;什么是 TPU？&lt;/h2&gt;
&lt;p&gt;TPU 本质上就是一个专为矩阵乘法设计的计算核心（叫做 TensorCore），旁边挂着一块超快的内存（叫 HBM，高带宽内存）。可以把它理解成一台超强的“矩阵乘法专用机”，不过它还有一些其他关键功能。&lt;/p&gt;
&lt;img src=&#34;https://pic2.zhimg.com/v2-72f975f51c397c6e5075b7d33f3cbd55_1440w.jpg&#34;&gt;
&lt;p&gt;TensorCore 主要包含三个重要单元：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;MXU（矩阵乘法单元，Matrix Multiply Unit）：这是整个 TensorCore 的核心。以 TPU v5e 为例，它每 8 个周期就能完成一次 bfloat16 [8, 128] × bfloat16 [128, 128] → float32 [8, 128] 的矩阵乘法，靠的是所谓的 systolic array 结构。在 1.5GHz 下，单个 MXU 每秒能跑约 5e13 次 bfloat16 运算。大多数 TensorCore 会配备 2 或 4 个 MXU，比如 TPU v5e 的总 FLOPs（bfloat16）就是 2e14。此外 TPU 还支持更低精度的矩阵乘法，比如 int8，可以达到更高的吞吐量（v5e 是 4e14 ops/s）。&lt;/li&gt;
&lt;li&gt;VPU（向量处理单元，Vector Processing Unit）：负责执行常见的数学操作，比如 ReLU 激活、点乘、加法、向量间的逐元素操作等。比如你要对一个张量做求和归一化这种事，也是在这里完成的。&lt;/li&gt;
&lt;li&gt;VMEM（向量内存，Vector Memory）：这是一块片上小内存，就挨着计算单元。虽然容量比 HBM 小得多（比如 v5e 是 128MiB），但访问速度非常快。它有点像 CPU 里的 L1/L2 缓存，但更大、而且由程序员手动控制。注意：TensorCore 不能直接从 HBM 读取数据做计算，数据必须先搬进 VMEM 才行。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;一句话总结：TPU 是专为深度学习设计的“矩阵加速器”，核心思想就是用超强的矩阵乘法引擎 + 超快的数据通路（VMEM + HBM）来提升训练速度。&lt;/p&gt;
&lt;p&gt;TPU 的强项就是矩阵乘法，而且是真的非常非常快。比如目前最强的 TPU 之一 —— TPU v5p，每个核心每秒可以跑 2.5e14 次 bfloat16 运算，一个芯片就是 5e14 次。如果你拿一个拥有 8960 颗芯片的大型 TPU pod 来算，一秒能跑 4 exaFLOPs，已经是世界级的超级计算机级别了，而且 Google 手上有很多这样的 pod。&lt;/p&gt;
&lt;p&gt;上面那张图还标了 SMEM 和 scalar unit，主要用来处理控制流相关的逻辑（比如 if-else、for loop），你不用特别关注，但有个重要的组件你一定要搞清楚：HBM（高带宽内存），这就是 TPU 存数据的地方，用来存张量。它的容量通常是几十 GB，比如 TPU v5e 是 16 GiB。每次你做计算的时候，数据都会从 HBM 通过VMEM（片上缓存）先搬到计算核心 MXU，然后计算完成后再从 VMEM 把结果写回 HBM。HBM 和 TensorCore 之间的带宽（就是通过 VMEM 通道的速度）通常是 1～2 TB/s，这个带宽是限制内存密集型任务计算速度的关键瓶颈，所以常被称为 HBM 带宽。&lt;/p&gt;
&lt;p&gt;不过别担心，TPU 在设计上是很智能的 —— 所有操作都是 流水线式并行处理。举个例子，要做矩阵乘法 Y = X @ A，TPU 会：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;先把部分的 X 和 A 从 HBM 搬到 VMEM；&lt;/li&gt;
&lt;li&gt;然后把 VMEM 里的数据喂给 MXU 来做乘法（比如 X 是 8×128 块，A 是 128×128 块）；&lt;/li&gt;
&lt;li&gt;然后把乘好的结果块再写回 HBM。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;而这一整套过程是分阶段交叉进行的，比如：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在计算当前一块的时候，就已经在搬下一块进 VMEM；&lt;/li&gt;
&lt;li&gt;计算完当前一块的时候，也在把之前算好的结果写回 HBM。&lt;/li&gt;
&lt;/ol&gt;
&lt;img src=&#34;https://jax-ml.github.io/scaling-book/assets/img/pointwise-product.gif&#34; loop=&#34;true&#34;&gt;
&lt;p&gt;通过这种“搬 + 算 + 写”三管齐下的流水线策略，TPU 能够最大程度保证 MXU 时刻有活干，避免它闲着等内存，从而维持计算瓶颈（compute-bound）状态，而不是被内存拖慢（memory-bound）。TPU 之所以能打，就是靠 MXU 算得快、HBM 送得快、VMEM 做缓存中转，再加上流水线式调度，几乎把硬件压榨到了极致。&lt;/p&gt;
&lt;p&gt;矩阵乘法（matmul）的数据流动路径其实和普通的点操作差不多，唯一的不同是它加载到的是 MXU，而不是 VPU。另外，由于同一块权重会被多个输入激活块反复使用，所以 matmul 的加载顺序也和点操作不同。你可以想象是：数据从 HBM 流入 VMEM，再进到 VREGs（向量寄存器），然后喂给向量单元或矩阵乘单元（VPU 或 MXU），最后结果再从 VMEM 写回到 HBM。但问题是：如果 HBM → VMEM 的速度跟不上计算单元（VPU/MXU） 的吞吐能力，那计算单元就“饿了”，得不到足够的数据，就浪费了计算力，这种情况叫“带宽瓶颈”（bandwidth-bound）。&lt;/p&gt;
&lt;h3 id=&#34;重点总结一下&#34;&gt;重点总结一下&lt;/h3&gt;
&lt;p&gt;TPU 架构很简单：先把数据从 HBM 拉到 VMEM；然后 VMEM 把数据喂给 systolic array（一个高效做乘加的阵列）；每秒能跑 200 万亿次乘加（200 TFLOPs）；HBM ↔ VMEM 和 VMEM ↔ MXU 的带宽，决定了你的程序到底能跑多快。&lt;/p&gt;
&lt;h3 id=&#34;vmem-与-arithmetic-intensity-的关系&#34;&gt;VMEM 与 Arithmetic Intensity 的关系&lt;/h3&gt;
&lt;p&gt;虽然 VMEM 容量比 HBM 小很多，但带宽高得多（大概是 HBM 的 22 倍），这意味着：如果你能把所有的输入和输出都装进 VMEM，就大大降低了通信瓶颈；特别是当一个算法的 arithmetic intensity 本来就不高时（就是 FLOPs 比 byte 少的那种），只要能放进 VMEM，就还有可能跑得快。举个例子：在 HBM 中你可能需要每 byte 至少算 200 次才能不浪费资源，而在 VMEM 中只要算 10～20 次就够了。所以，如果你能把参数提前放进 VMEM，你就能用很小的 batch size 就达成高效率。问题是 VMEM 太小了，很多模型根本塞不下，这才是挑战。&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-9a374166dfdc03c394e602a0d0716d7b_1440w.jpg&#34;&gt;
&lt;h3 id=&#34;tpu-芯片和组网结构&#34;&gt;TPU 芯片和组网结构&lt;/h3&gt;
&lt;p&gt;一个 TPU 芯片通常有两个核心，共享内存，一起工作就叫做 megacore，从 TPU v4 开始都是这样。早期（v3 及以前）是两个独立核心，各自有自己的内存；面向推理的 TPU（如 v5e）只有一个核心，不搞共享。&lt;/p&gt;
&lt;img src=&#34;https://pic2.zhimg.com/v2-f84ef1363581b450c3e1d0fc736c17e3_1440w.jpg&#34;&gt;
&lt;p&gt;4 个芯片组成一个 tray（托盘），通过 PCIe 网络连到一个 CPU 主机，很多熟悉 Google Colab 或 TPU VM 的用户看到的就是这种 4 芯片、8 核心配置。推理版的 TPU v5e 是 2 个托盘一个主机，但每芯片只有 1 个核心，所以是 8 芯片 8 核心。&lt;/p&gt;
&lt;img src=&#34;https://pic3.zhimg.com/v2-40dbb423f6e0cc8f86372f7a3d1797ba_1440w.jpg&#34;&gt;
&lt;p&gt;不过PCIe 是个限制项：和 HBM ↔ VMEM 一样，CPU ↔ HBM 之间通过 PCIe 连接，它也有带宽限制。比如 TPU v4 的 PCIe 带宽只有 16GB/s（单向），比 HBM 慢了快 100 倍；所以虽然你可以从主机内存搬数据进 TPU，但速度慢得很，不适合频繁交换。&lt;/p&gt;
&lt;h2 id=&#34;tpu-网络结构&#34;&gt;TPU 网络结构&lt;/h2&gt;
&lt;p&gt;TPU 芯片之间通过叫做 ICI 网络（Inter-Chip Interconnect） 的通道连接起来。在一个 TPU Pod 中，不同芯片直接用 ICI 连着，不需要经过主机（host），效率非常高。对于老一代 TPU（v2 / v3） 、推理芯片（v5e）以及 v6e（Trillium），每个芯片跟4 个最近的邻居连接，构成一个 2D 环形网络（2D torus），两端用“边缘连接”封住，组成环。对于 TPU v4 / v5p，每个芯片和 6 个邻居连接，组成一个 3D 环形网络（3D torus）；&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-cbb8dc282aeebd6de14af07ea81a6dcf_1440w.jpg&#34;&gt;
&lt;p&gt;这种“环形结构（Torus）”的好处是把任意两个芯片之间的最远距离从 N 缩短到了 N/2，数据传输更快、更稳定。有些网络还用了所谓的 “扭曲环 Mobius” 结构，类似莫比乌斯带，进一步减少了芯片间的平均通信距离（这太 geek 了，有兴趣可以自己搜图）。&lt;/p&gt;
&lt;h3 id=&#34;tpu-pod-和-superpod&#34;&gt;TPU Pod 和 SuperPod&lt;/h3&gt;
&lt;p&gt;TPU 是按 “Pod” 来组的，Pod 内部通过 ICI 连接。Pod 可以特别大，比如 TPU v4 的最大 Pod 是 16×16×16 个芯片，TPU v5p 最大可以达到 16×20×28。这么大的网络其实是通过 4×4×4 的芯片小方块连接而成，中间用光学交换器做 “wraparound” 连起来（有点像包裹起来的立方体）。这些方块可以重新配置，实现灵活的超大规模拓扑结构。&lt;/p&gt;
&lt;img src=&#34;https://pica.zhimg.com/v2-58f7e70afd60e851450a28226950bffe_1440w.jpg&#34;&gt;
&lt;p&gt;你也可以请求一个小规模拓扑，比如 2×2×1、2×2×2，但这种小拓扑没 wraparound，通信效率就低了。没有 wraparound 的情况下，通信延迟通常会翻倍，所以建议尽量选 4×4×4 或它的倍数这种有 wrap 的结构。&lt;/p&gt;
&lt;img src=&#34;https://pic2.zhimg.com/v2-dc9e0800334b394a529b8bcccb4c3e47_1440w.jpg&#34;&gt;
&lt;p&gt;对于TPU v5e 和 Trillium（v6e），它们是专门为推理优化的，构成一个 16×16 的 2D torus；只要某一维有 16，就会自动有 wraparound，不支持超出 16×16 的拓展。但可以通过 DCN（Data Center Network） 实现跨 Pod 通信，DCN 是 主机 ↔ 主机 的通道，速度比 ICI 慢多了。&lt;/p&gt;
&lt;img src=&#34;https://pic4.zhimg.com/v2-35617fb9c9ed504d00c2fd31fba59e77_1440w.jpg&#34;&gt;
&lt;p&gt;TPU 和 GPU 在连接方式上有挺大的差别。TPU 是那种“邻居互联”的风格，每个芯片只连它周围的几个芯片，像搭积木一样拼成一个二维或三维的“环形网络”（torus）。这种连接方式很简单、便宜，而且扩展性特别好。相比之下，GPU 用的是一套更复杂的“全连接近似”结构，靠一堆 NVLink 交换机把所有 GPU 尽可能都连起来。这样做虽然让同一个节点里的 GPU（比如一台 8 卡的 H100 机器）之间通信特别快，但一旦规模上去，通信就得走好几级交换路径，延迟和成本都会飙升。&lt;/p&gt;
&lt;p&gt;TPU 的思路就不一样了：它把每个芯片之间的通信带宽和连接数都固定下来，这样整个系统搭建起来既便宜又统一，比如 TPU v5p 每个芯片的 HBM 带宽有 2.5TB/s，芯片之间的 ICI 链路大概每个方向 90GB/s，而真正跨主机通信的 DCN（数据中心网络）只有可怜的 25GB/s。更关键的是，一个主机通常带 8 个 TPU 芯片，所以平摊下来，每个芯片能用的 DCN 带宽其实只有 3GB/s 出头，远远不如 ICI，更别提 HBM 了。&lt;/p&gt;
&lt;p&gt;这就导致一个问题：如果你把模型切分到多个 TPU 芯片上运行，尤其是分布在不同主机上的时候，如果通信规划不好，很容易卡在带宽不够这件事上。特别是在“multi-slice”这种跨多个 TPU 分组训练的场景下，一组 ICI 相连的 TPU 叫做一个 slice，slice 之间的通信必须走 DCN。而 DCN 这条路非常绕：数据得先从 TPU 走 PCIe 到主机，再通过网络发出去，到了目标主机后再走 PCIe 写进目标 TPU 的 HBM，整个流程特别慢。所以训练时最理想的方式，是尽量把大部分通信压在 ICI 这一层，避免走 DCN 这条“慢车道”。这样才能让 TPU 的矩阵乘法单元（MXU）不被通信拖后腿，真正跑满算力。&lt;/p&gt;
&lt;h2 id=&#34;经典总结&#34;&gt;经典总结&lt;/h2&gt;
&lt;p&gt;好了，晦涩的文字到此结束，总结一下：&lt;/p&gt;
&lt;p&gt;TPU 本质上就是一个特别强的“矩阵乘法机器”，它连着三样东西：超快的内存（HBM）、周围几个“邻居芯片”（通过 ICI）、还有数据中心里其他机器（通过 DCN）。&lt;/p&gt;
&lt;p&gt;这三种通信通道的速度从快到慢分别是：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;HBM 带宽：就是 TensorCore 和它本地内存之间的传输，非常快；&lt;/li&gt;
&lt;li&gt;ICI 带宽：芯片之间的通信，比 HBM 慢一点，但还挺快的；&lt;/li&gt;
&lt;li&gt;PCIe 带宽：主机和芯片托盘之间的连接，速度一般；&lt;/li&gt;
&lt;li&gt;DCN 带宽：多个主机之间通过数据中心网络通信，最慢。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;在一个 slice 里（也就是一组 ICI 相连的 TPU），芯片之间只跟最邻近的芯片连接。如果你要让两个距离远的芯片通信，它们得一跳一跳地转发，挺费时间的。&lt;/p&gt;
&lt;p&gt;此外，做矩阵乘法的时候，权重矩阵的两个维度最小都得填充到 128（TPU v6 上是 256），这样才能把 MXU（矩阵乘单元）用满。&lt;/p&gt;
&lt;p&gt;还有一点是：精度越低，运算越快。TPU 支持 int8 和 int4，这比用 bfloat16 快两倍甚至四倍。当然啦，VPU（向量处理器）做的那些操作，比如激活函数啥的，还是用 fp32。&lt;/p&gt;
&lt;p&gt;最后一条也很关键：为了不让 TPU 的算力闲着，我们得让不同通信通道上的数据量跟它们各自的速度匹配上。也就是说：快的通道多跑点，慢的通道少跑点，不然就拖后腿了。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>大模型训练1：模型训练的时间都花在哪了？</title>
      <link>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%831/</link>
      <pubDate>Wed, 30 Jul 2025 00:00:00 +0000</pubDate>
      <guid>http://localhost:1313/blog/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E8%AE%AD%E7%BB%831/</guid>
      <description>&lt;!--  $$ 
 --&gt;
&lt;p&gt;随着模型规模越来越大，大模型时代的工程能力和 research 能力逐渐变得同等重要。还记得几年前做科研经常看到一些paper改几行matlab、python，就能取得有效的的 performance。不过在当下，我估计很难了。现如今大模型的训练、复杂的代码嵌套和各种工程挑战，我觉得对曾经做学术的人来说都不是很友好。我自己也深有体会，接触 Megatron-LM 之后，整个人都变得沉静了。
恰好最近看到一个博客，详细地介绍了训练大模型的各种技术。我看完之后，感觉深受启发，因此写一版自己对其的理解与反思，权当记录，也乐得分享，若对读者也能有所帮助，也十分荣幸。&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://jax-ml.github.io/scaling-book/&#34;&gt;How to Scale Your Model&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;训练LLM 常常让人感觉像是在搞炼金术，但其实理解和优化模型性能并没有那么神秘。这个 blog 的目标就是帮你揭开扩展语言模型背后的科学原理：TPU（还有 GPU）到底是怎么工作的，它们之间是怎么通信的，LLM 在真实硬件上是如何运行的，以及在训练和推理过程中，如何把模型合理并行化，让它能在大规模场景下高效运行。如果你曾经有过这些疑问：“训练一个 LLM 到底得花多少钱？”“我要自己部署这个模型，需要多大的内存？”“AllGather 到底是啥玩意？”那这些内容，应该会对你有帮助。&lt;/p&gt;
&lt;p&gt;当我们在硬件上跑算法时，性能主要受到三个方面的限制：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;计算速度 —— 也就是机器做数学运算的能力，比如每秒能处理多少次操作（OPs/second）&lt;/li&gt;
&lt;li&gt;带宽 —— 数据在内存、缓存、芯片之间搬运的速度（bytes/second）&lt;/li&gt;
&lt;li&gt;总内存容量 —— 也就是设备最多能装下多少数据（bytes）&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;这些限制就像给计算画出了一条 Roofline：上面顶着计算能力，下面托着内存和带宽瓶颈。通过这套 Roofline 模型，我们可以大致估算出某段计算最快能跑多快（上限）和最慢会卡在哪（下限）。&lt;/p&gt;
&lt;p&gt;简单理解一下：如果算法数据太多、内存太小，那它就容易被“撑爆”；如果算法计算量很大但数据搬运太慢，那就等着“搬砖”；如果都不是问题，那就看计算核心有多能打了。这三点加起来，就决定了你模型在硬件上到底能跑多快、扩多大。后面我们会用这个模型来分析怎么高效地扩展模型到更大的硬件，比如 TPUs。&lt;/p&gt;
&lt;h2 id=&#34;训练模型哪里需要花时间&#34;&gt;训练模型，哪里需要花时间？&lt;/h2&gt;
&lt;p&gt;我们运行一个模型，花时间的地方主要有三类：&lt;/p&gt;
&lt;h3 id=&#34;1-计算computation&#34;&gt;1. 计算（Computation）&lt;/h3&gt;
&lt;p&gt;深度学习模型本质上就是一堆矩阵乘法，每个矩阵乘法由大量浮点加法和乘法组成，也就是所谓的 FLOPs（floating point operations）。我们使用的加速卡（GPU 或者 TPU）处理 FLOPs 的速度决定了计算所需要的时间。这里我们定义计算时间  $T_{math}$ 
 为&lt;/p&gt;
 $$T_{math} = \frac{\rm Computation \ FLOPs}{\rm Accelerator \ FLOPs/s}$$ 

&lt;p&gt;举个例子直观理解一下，NVIDIA H100 每秒可以完成大约 9.89e14 个 bfloat16 的 FLOPs，TPU v6e 也差不多是 9.1e14。如果你有一个模型需要做 1e12 个 FLOPs，那在 H100 上大概只需要 1e12 / 9.89e14 = 1.01ms，在 TPU v6e 上大概是 1.1ms。&lt;/p&gt;
&lt;p&gt;这告诉我们，只算数学运算的话，其实模型是可以很快的。&lt;/p&gt;
&lt;h3 id=&#34;2-芯片内部的通信communication-within-a-chip&#34;&gt;2. 芯片内部的通信（Communication within a chip）&lt;/h3&gt;
&lt;p&gt;模型运行过程中，张量需要在芯片的内存（比如 HBM）和计算核心之间传输。这个传输速度叫 HBM 带宽。对于H100来说，这大约是 3.35TB/s；而对 TPU v6e，大约是 1.6TB/s。
这部分搬数据的时间也需要算进去，特别是当数据量比较大时。&lt;/p&gt;
&lt;h3 id=&#34;3-芯片之间的通信communication-between-chips&#34;&gt;3. 芯片之间的通信（Communication between chips）&lt;/h3&gt;
&lt;p&gt;当你的模型太大，需要多个加速卡（比如多个 GPU 或 TPU）协作时，张量还需要在不同的芯片之间传来传去。不同的连接方式（比如 ICI, DCN, PCIe）速度不一样，单位和芯片内部通信速度一样，仍然是 bytes/second。到这里，我们也给通信时间  $$ 
 做一个定义：&lt;/p&gt;
 $$T_{comm} = \frac{\rm Computation \ Bytes}{\rm Network/Memory \ Bandwidth \  Bytes/s}$$ 

&lt;h3 id=&#34;估算总时间的方式&#34;&gt;估算总时间的方式&lt;/h3&gt;
&lt;p&gt;无论是计算还是通信，我们都可以粗略估计所需时间。并且：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;理论下限（Lower bound）：就是计算和通信中耗时更长的那个；&lt;/li&gt;
&lt;li&gt;理论上限（Upper bound）：是两者耗时之和。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;大多数时候，我们可以通过“通信和计算并行”来让实际耗时更接近下限。因此，优化的目标通常是让通信时间和计算时间尽可能重叠。而即使最坏的情况，也只是差一个因子 2（也就是最长不会慢两倍）。另外，如果计算时间远大于通信时间，那就说明硬件一直在计算、没有等数据，说明我们主要受限于计算能力，处于计算瓶颈（compute-bound）状态；如果反过来是通信时间大于计算时间，那就是我们大部分时间都在“等数据”，说明被通信瓶颈（communication-bound）限制了，FLOPs 的潜力没有完全发挥出来，有浪费。那么我们怎么判断一个操作是计算瓶颈还是通讯瓶颈呢？&lt;/p&gt;
&lt;h3 id=&#34;算术强度&#34;&gt;算术强度&lt;/h3&gt;
&lt;p&gt;看一个关键指标：算术强度（Arithmetic Intensity），也叫 Operational Intensity。&lt;/p&gt;
 $${\rm Arithmetic \ Intensity} = \frac{\rm Computation \ FLOPs}{\rm Computation\ Bytes}$$ 

&lt;p&gt;注意这里的“通信”既包括芯片内部（如 HBM）也包括芯片之间（如 GPU-GPU）。这个值就表示：“每搬一个字节，能做多少次浮点运算”。直觉上来理解，如果算术强度高：说明每搬一次数据能做很多计算，计算时间就占主导，是计算瓶颈；如果算术强度低，说明大部分时间都在搬数据，计算单元在空转，是通信瓶颈；而这两者“谁主谁次”的拐点，就叫 peak arithmetic intensity（峰值算术强度），也就是：peak FLOPs/s ÷ 带宽（bytes/s）。&lt;/p&gt;
&lt;p&gt;峰值算术强度是硬件的一个特性。我们以TPU v5e 为例，它最大计算能力是 1.97e14 FLOPs/s，带宽是 8.2e11 bytes/s，所以它的峰值算术强度大约是1.97e14 / 8.2e11 ≈ 240 FLOPs/byte，这就意味着，如果一个算法的算术强度低于 240，它会变成通信瓶颈。以点积为例，计算两个向量 x*y，假设它们的长度都是 N，每个元素是 bfloat16：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;要从内存读出 x 和 y，每个都是2N 字节，一共 4N 字节（注意：bf16 每个元素是 2 字节，所以这里乘以 2）；&lt;/li&gt;
&lt;li&gt;做 N 次乘法，N-1 次加法，共 2N-1 个 FLOPs；&lt;/li&gt;
&lt;li&gt;最后再写回 2 字节。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;当 N 趋近于无穷大时，整体的算术强度 (2N-1) FLOPs / （4N+2） bytes 近似为 0.5 FLOPs/byte，这个值远远小于 TPU 的 240 FLOPs/byte，所以这个操作是通信瓶颈，说明你即使硬件很强，也没法发挥它的全部计算力。&lt;/p&gt;
&lt;p&gt;总结一句话：算术强度就是告诉你：你搬的每一份数据，能不能被“吃干榨净”。如果搬运得多，算得少，硬件再强也浪费；如果算得多，搬得少，那你就能用满算力。这就是判断计算 vs 通信瓶颈的核心标准。&lt;/p&gt;
&lt;h3 id=&#34;roofline&#34;&gt;Roofline&lt;/h3&gt;
&lt;p&gt;我们可以用一种叫做 Roofline 图的方式，把计算能力和内存带宽的权衡形象地画出来。这个图是个对数坐标图，横轴是算术强度（FLOPs per byte），纵轴是你在特定硬件上能达到的最大吞吐量（FLOPs/s）。&lt;/p&gt;
&lt;img src=&#34;https://picx.zhimg.com/v2-8969d4159382fc78037b685e2f8a43a5_1440w.jpg&#34;&gt;
&lt;p&gt;图上会看到几条线：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;红色区域：算法的算术强度太低，无论你的带宽是 BW1 还是 BW2，都会被带宽限制住，硬件的 FLOPs 没有被用满。&lt;/li&gt;
&lt;li&gt;黄色区域：算法在低带宽（BW1）下受限，但如果你换个高带宽（BW2）就能跑快。&lt;/li&gt;
&lt;li&gt;绿色区域：算术强度足够高，不再受内存带宽影响，这时瓶颈变成了计算能力，硬件跑满了。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;图中也有两个算法：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Algo 1（左边）：算术强度低，受限于内存带宽，只用到了硬件一小部分计算力，是通信瓶颈。&lt;/li&gt;
&lt;li&gt;Algo 2（右边）：算术强度高，达到了硬件的峰值 FLOPs/s，是计算瓶颈，充分利用了硬件。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这张图告诉我们：如果一个算法在红区，你可以通过提高算术强度（比如增加计算量、减少内存访问）或者提高内存带宽来让它更快；如果一个算法已经在绿区，那再提带宽或者强度没啥意义了，因为已经计算瓶颈了。&lt;/p&gt;
&lt;h3 id=&#34;矩阵乘法&#34;&gt;矩阵乘法&lt;/h3&gt;
&lt;p&gt;讲了这么多，我们来看一个实际的应用。我们考虑最常见的算法之一：矩阵乘法（matrix multiplication，简称 matmul）。&lt;/p&gt;
&lt;p&gt;假设你有两个矩阵：X，大小 bf16[B, D]，Y，大小 bf16[D, F]，得到结果矩阵 Z bf16[B, F]。为了计算这个 matmul，你得从内存里读取 2DF + 2BD 字节的数据，执行 2BDF 个 FLOPs（每次乘法+加法）；然后把 Z 写回内存，要写 2BF 字节。&lt;/p&gt;
&lt;p&gt;如果我们假设 B（batch size）远小于 D 和 F，也就是 token 数相对 embedding size 和头数小得多（这在 Transformer 中很常见），那么算术强度大约就是：&lt;/p&gt;
 $$\text{Arithmetic Intensity} = \frac{2BDF}{2DF + 2BD + 2BF} \approx B$$ 

&lt;p&gt;换句话说，这个强度跟 batch size 成正比。所以，在 TPU v5e 上，如果你用的是 bfloat16 类型，那么只要你的 batch size 超过 240 个 token，你就可以做到 compute-bound。&lt;/p&gt;
&lt;p&gt;当然，这些都是单卡内的 memory-bandwidth 限制，也就是看显存带宽能不能喂饱计算。但这其实只是最简单的一类 roofline，现实中我们更常遇到的瓶颈是多卡之间的通信带宽，尤其是在多个 TPU/GPU 上做分布式矩阵乘法时。来举个例子，还是原来的 X 和 Y，你把它们在维度 D 上一分为二，分别放在两张卡（比如 2 个 TPU）。现在你要做矩阵乘法，做法如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;在 TPU 0 上：算前一半： A = X[:, :D//2] @ Y[:D//2, :]&lt;/li&gt;
&lt;li&gt;在 TPU 1 上：算后一半： B = X[:, D//2:] @ Y[D//2:, :]&lt;/li&gt;
&lt;li&gt;然后两张卡交换 A 和 B 的结果，把它们加起来得到最终输出。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;对于这一次，每张卡只做一半的工作，所以计算时间是原来的一半：&lt;/p&gt;
 $$T_{math} = \frac{BDF}{1.97e14}$$ 

&lt;p&gt;而通信时间是两个卡交换结果的时间，也就是：&lt;/p&gt;
 $$T_{comm} = \frac{2BF}{4.5e10}$$ 

&lt;p&gt;我们要找出什么时候通信时间小于计算时间（也就是还能跑满卡，不受通信限制），解这个不等式：&lt;/p&gt;
 $$ \frac{BDF}{1.97e14} &gt; \frac{2BF}{4.5e10} \Rightarrow \frac{D}{2} &gt; \frac{1.97e14}{4.5e10} = 4377 \Rightarrow D &gt; 8755$$ 

&lt;p&gt;也就是说：只要你的 embedding size D &amp;gt; 8755，通信就不是瓶颈，你是 compute-bound；反之就是 communication-bound。在这个场景下，决定是否 compute-bound 的，不是 batch size B，而是特征维度 D。&lt;/p&gt;
&lt;p&gt;多卡分布式计算下的 roofline 限制关键在于通信带宽，是否能充分利用硬件取决于通信 vs. 计算的比例 —— 而这个比例依赖于模型维度，不是 batch size。理解这个规律，才能知道你什么时候该切分模型、怎么切分才能不踩通信瓶颈。&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>
